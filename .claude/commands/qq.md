Use the QQ bridge status tool to check whether the QQ channel is connected.

If a QQ message arrives as a channel notification, answer with the `reply`
tool and pass through the inbound `chat_id`. The QQ sender cannot see normal
terminal output.
